<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Bibsie Boys Sodoku Saga</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Bangers&family=Noto+Sans+JP:wght@700&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            -webkit-tap-highlight-color: transparent;
        }
        
        :root {
            --neon-red: #FF2D55;
            --hot-pink: #FF6B9D;
            --deep-purple: #2D0A1F;
            --gold: #FFD700;
            --stone: #4A4A4A;
            --stone-light: #7A7A7A;
        }
        
        body {
            font-family: 'Bangers', cursive;
            background: var(--deep-purple);
            min-height: 100vh;
            overflow: hidden;
            touch-action: manipulation;
        }
        
        /* Video Intro Screen */
        .video-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: black;
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 1000;
            flex-direction: column;
        }
        
        .video-screen.hidden {
            display: none;
        }
        
        #introVideo {
            width: 100%;
            height: 100%;
            object-fit: contain;
            max-height: 100vh;
        }
        
        .skip-btn {
            position: absolute;
            bottom: 50px;
            right: 30px;
            background: rgba(255,45,85,0.8);
            border: 2px solid white;
            color: white;
            font-family: 'Bangers', cursive;
            font-size: 1.2rem;
            padding: 10px 25px;
            border-radius: 30px;
            cursor: pointer;
            z-index: 1001;
            transition: all 0.3s;
        }
        
        .skip-btn:hover {
            background: var(--neon-red);
            transform: scale(1.05);
        }
        
        /* Wolken Hintergrund */
        .clouds-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('clouds.png') center/cover;
            opacity: 0.8;
            z-index: 0;
            animation: cloudMove 30s infinite alternate;
        }
        
        @keyframes cloudMove {
            0% { transform: scale(1); }
            100% { transform: scale(1.1); }
        }
        
        /* Screens */
        .screen {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            z-index: 10;
            padding: 20px;
        }
        
        .screen.active {
            display: flex;
        }
        
        /* Title Screen mit Poster */
        .title-screen-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('poster.png') center/cover;
            z-index: -1;
        }
        
        .title-overlay {
            background: rgba(45, 10, 31, 0.7);
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }
        
        .title-container {
            text-align: center;
            z-index: 20;
            margin-top: 60vh;
        }
        
        .main-title {
            font-size: clamp(2.5rem, 10vw, 4rem);
            color: white;
            text-shadow: 
                4px 4px 0px var(--neon-red),
                8px 8px 20px rgba(255,45,85,0.8);
            letter-spacing: 2px;
            line-height: 1;
            margin-bottom: 10px;
            animation: titlePulse 2s infinite;
        }
        
        @keyframes titlePulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.02); }
        }
        
        .sub-title {
            font-family: 'Noto Sans JP', sans-serif;
            font-size: clamp(1rem, 4vw, 1.5rem);
            color: var(--hot-pink);
            text-shadow: 2px 2px 4px rgba(0,0,0,0.8);
            margin-bottom: 30px;
        }
        
        .btn {
            background: linear-gradient(135deg, var(--neon-red) 0%, var(--hot-pink) 100%);
            border: 3px solid white;
            color: white;
            font-family: 'Bangers', cursive;
            font-size: clamp(1.2rem, 5vw, 1.8rem);
            padding: 15px 40px;
            border-radius: 50px;
            cursor: pointer;
            box-shadow: 
                0 0 20px rgba(255,45,85,0.6),
                inset 0 0 20px rgba(255,255,255,0.2);
            transition: all 0.3s;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin: 10px;
            position: relative;
            overflow: hidden;
        }
        
        .btn:hover, .btn:active {
            transform: scale(1.05);
            box-shadow: 
                0 0 40px rgba(255,45,85,0.8),
                inset 0 0 30px rgba(255,255,255,0.3);
        }
        
        /* Level Select */
        .level-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 15px;
            max-width: 400px;
            width: 100%;
            margin: 20px 0;
            z-index: 20;
        }
        
        .level-btn {
            aspect-ratio: 1;
            border-radius: 15px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            position: relative;
            overflow: hidden;
            border: 3px solid;
            transition: all 0.3s;
            background: rgba(45, 10, 31, 0.8);
            backdrop-filter: blur(10px);
        }
        
        .level-btn.locked {
            border-color: var(--stone-light);
            color: #666;
            cursor: not-allowed;
            opacity: 0.6;
        }
        
        .level-btn.available {
            border-color: var(--neon-red);
            color: white;
            cursor: pointer;
            box-shadow: 0 0 15px rgba(255,45,85,0.5);
            animation: levelPulse 2s infinite;
        }
        
        .level-btn.completed {
            border-color: #00C853;
            color: white;
            cursor: pointer;
            background: rgba(0, 200, 83, 0.3);
        }
        
        .level-btn.completed::after {
            content: '💖';
            position: absolute;
            top: 5px;
            right: 5px;
            font-size: 1rem;
            animation: heartFloat 2s infinite;
        }
        
        @keyframes levelPulse {
            0%, 100% { box-shadow: 0 0 10px rgba(255,45,85,0.5); }
            50% { box-shadow: 0 0 25px rgba(255,45,85,0.8); }
        }
        
        @keyframes heartFloat {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-5px); }
        }
        
        .level-btn.final {
            background: linear-gradient(135deg, var(--gold) 0%, #FFA000 100%);
            border-color: white;
            color: var(--deep-purple);
            grid-column: span 3;
            aspect-ratio: 3/1;
            font-weight: bold;
        }
        
        /* Game Screen */
        .game-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
            max-width: 500px;
            margin-bottom: 15px;
            color: white;
            font-size: 1.2rem;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.8);
            z-index: 20;
        }
        
        .level-title {
            font-size: 1.5rem;
            color: var(--gold);
            text-shadow: 0 0 10px rgba(255,215,0,0.5);
        }
        
        .hero-display {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-bottom: 20px;
            z-index: 20;
        }
        
        .hero-img {
            width: 80px;
            height: 120px;
            object-fit: contain;
            filter: drop-shadow(0 5px 15px rgba(255,45,85,0.6));
            animation: heroFloat 3s infinite ease-in-out;
        }
        
        @keyframes heroFloat {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }
        
        .platform-img {
            width: 100px;
            height: 60px;
            object-fit: contain;
            opacity: 0.8;
        }
        
        .sudoku-container {
            position: relative;
            background: rgba(255,255,255,0.1);
            padding: 15px;
            border-radius: 20px;
            box-shadow: 
                0 0 30px rgba(255,45,85,0.3),
                inset 0 0 30px rgba(255,255,255,0.1);
            backdrop-filter: blur(10px);
            z-index: 20;
        }
        
        .sudoku-grid {
            display: grid;
            grid-template-columns: repeat(9, 1fr);
            gap: 2px;
            background: var(--neon-red);
            padding: 3px;
            border-radius: 10px;
            box-shadow: 
                0 0 20px rgba(255,45,85,0.6),
                inset 0 0 10px rgba(255,255,255,0.2);
            max-width: 90vw;
            width: 400px;
            background-image: url('grid.png');
            background-size: cover;
        }
        
        .cell {
            aspect-ratio: 1;
            background: rgba(45,10,31,0.95);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: clamp(1.2rem, 5vw, 1.8rem);
            color: white;
            cursor: pointer;
            transition: all 0.2s;
            position: relative;
            border: 1px solid rgba(255,45,85,0.3);
        }
        
        .cell:nth-child(3n) {
            border-right: 2px solid var(--neon-red);
        }
        
        .cell:nth-child(n+19):nth-child(-n+27),
        .cell:nth-child(n+46):nth-child(-n+54) {
            border-bottom: 2px solid var(--neon-red);
        }
        
        .cell.fixed {
            background: rgba(255,45,85,0.4);
            font-weight: bold;
            color: var(--hot-pink);
        }
        
        .cell.selected {
            background: rgba(255,215,0,0.4);
            box-shadow: inset 0 0 15px rgba(255,215,0,0.6);
            animation: cellPulse 1s infinite;
        }
        
        .cell.error {
            background: rgba(255,0,0,0.4);
            animation: shake 0.5s;
        }
        
        @keyframes cellPulse {
            0%, 100% { box-shadow: inset 0 0 10px rgba(255,215,0,0.4); }
            50% { box-shadow: inset 0 0 20px rgba(255,215,0,0.8); }
        }
        
        @keyframes shake {
            0%, 100% { transform: translateX(0); }
            25% { transform: translateX(-5px); }
            75% { transform: translateX(5px); }
        }
        
        .number-pad {
            display: grid;
            grid-template-columns: repeat(5, 1fr);
            gap: 10px;
            margin-top: 20px;
            max-width: 400px;
            width: 100%;
            z-index: 20;
        }
        
        .num-btn {
            aspect-ratio: 1;
            border-radius: 15px;
            border: 2px solid var(--neon-red);
            background: rgba(255,45,85,0.3);
            color: white;
            font-family: 'Bangers', cursive;
            font-size: 1.5rem;
            cursor: pointer;
            transition: all 0.2s;
            display: flex;
            align-items: center;
            justify-content: center;
            backdrop-filter: blur(5px);
        }
        
        .num-btn:hover, .num-btn:active {
            background: var(--neon-red);
            transform: scale(1.1);
            box-shadow: 0 0 15px rgba(255,45,85,0.8);
        }
        
        .num-btn.action {
            background: rgba(255,255,255,0.2);
            border-color: white;
            font-size: 1rem;
        }
        
        /* Win Animation */
        .win-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.85);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 100;
            flex-direction: column;
        }
        
        .win-overlay.active {
            display: flex;
            animation: fadeIn 0.5s;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        
        .heart-explosion {
            font-size: 8rem;
            animation: heartExplode 2s ease-out;
            filter: drop-shadow(0 0 50px rgba(255,107,157,1));
        }
        
        @keyframes heartExplode {
            0% { transform: scale(0) rotate(0deg); opacity: 0; }
            50% { transform: scale(1.5) rotate(180deg); }
            100% { transform: scale(1) rotate(360deg); opacity: 1; }
        }
        
        .win-text {
            font-size: 2.5rem;
            color: white;
            text-align: center;
            margin: 20px;
            text-shadow: 
                0 0 20px var(--hot-pink),
                0 0 40px var(--neon-red);
            animation: textGlow 2s infinite;
        }
        
        @keyframes textGlow {
            0%, 100% { text-shadow: 0 0 20px var(--hot-pink), 0 0 40px var(--neon-red); }
            50% { text-shadow: 0 0 40px var(--hot-pink), 0 0 80px var(--neon-red); }
        }
        
        .win-subtext {
            font-family: 'Noto Sans JP', sans-serif;
            font-size: 1.2rem;
            color: var(--hot-pink);
            margin-bottom: 20px;
        }
        
        /* Final Screen */
        .final-container {
            text-align: center;
            z-index: 20;
        }
        
        .princess-emoji {
            font-size: 6rem;
            animation: princessFloat 3s infinite ease-in-out;
            filter: drop-shadow(0 0 30px rgba(255,215,0,0.8));
        }
        
        @keyframes princessFloat {
            0%, 100% { transform: translateY(0) rotate(-5deg); }
            50% { transform: translateY(-20px) rotate(5deg); }
        }
        
        .love-connection {
            font-size: 4rem;
            margin: 20px;
            animation: pulse 1s infinite;
        }
        
        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.2); }
        }
        
        .credits-box {
            background: rgba(255,215,0,0.2);
            border: 3px solid var(--gold);
            border-radius: 20px;
            padding: 30px;
            margin: 20px;
            backdrop-filter: blur(10px);
        }
        
        .credits-box h2 {
            font-size: 2rem;
            color: var(--gold);
            margin-bottom: 15px;
            text-shadow: 0 0 20px rgba(255,215,0,0.5);
        }
        
        .credits-box p {
            color: white;
            font-family: 'Noto Sans JP', sans-serif;
            font-size: 1rem;
            line-height: 1.6;
        }
        
        /* Particles */
        .particle {
            position: fixed;
            pointer-events: none;
            z-index: 50;
        }
        
        .heart-particle {
            color: var(--hot-pink);
            font-size: 2rem;
            animation: particleFloat 3s ease-out forwards;
        }
        
        @keyframes particleFloat {
            0% { 
                transform: translateY(0) scale(1); 
                opacity: 1; 
            }
            100% { 
                transform: translateY(-100vh) scale(0.5); 
                opacity: 0; 
            }
        }
        
        /* Responsive */
        @media (max-width: 480px) {
            .sudoku-grid {
                width: 95vw;
            }
            .cell {
                font-size: 1.2rem;
            }
            .hero-img {
                width: 60px;
                height: 90px;
            }
        }
    </style>
</head>
<body>
    <!-- VIDEO INTRO SCREEN -->
    <div class="video-screen" id="videoScreen">
        <video id="introVideo" playsinline autoplay muted>
            <source src="intro.mp4" type="video/mp4">
            Dein Browser unterstützt kein Video.
        </video>
        <button class="skip-btn" onclick="skipVideo()">ÜBERSPRINGEN ⏭</button>
    </div>
    
    <!-- Wolken Hintergrund -->
    <div class="clouds-bg"></div>
    
    <!-- TITLE SCREEN -->
    <div class="screen" id="titleScreen">
        <div class="title-screen-bg"></div>
        <div class="title-overlay"></div>
        
        <div class="title-container">
            <h1 class="main-title">BIBSIE BOYS<br>SODOKU SAGA</h1>
            <p class="sub-title">シードクサーガ</p>
            
            <p style="color: white; margin-bottom: 20px; font-size: 1.2rem; text-shadow: 2px 2px 4px rgba(0,0,0,0.8);">
                "Finde die Liebe in den Zahlen"
            </p>
            
            <button class="btn" onclick="showScreen('levelScreen')">START SAGA</button>
        </div>
    </div>
    
    <!-- LEVEL SELECT SCREEN -->
    <div class="screen" id="levelScreen">
        <h2 class="main-title" style="font-size: 2.5rem; margin-bottom: 10px; z-index: 20;">WÄHLE DEIN LEVEL</h2>
        <p class="sub-title" style="z-index: 20;">レベルを選択</p>
        
        <div class="level-grid" id="levelGrid">
            <!-- Wird dynamisch generiert -->
        </div>
        
        <button class="btn" onclick="showScreen('titleScreen')" style="margin-top: 20px; z-index: 20;">ZURÜCK</button>
    </div>
    
    <!-- GAME SCREEN -->
    <div class="screen" id="gameScreen">
        <div class="game-header">
            <span id="levelDisplay">LEVEL 1</span>
            <span class="level-title" id="levelName">ROSA ANFANG</span>
            <span id="timer">00:00</span>
        </div>
        
        <div class="hero-display">
            <img src="hero.png" alt="Bibsie Boy" class="hero-img" id="gameHero">
            <img src="platform.png" alt="Platform" class="platform-img">
        </div>
        
        <div class="sudoku-container">
            <div class="sudoku-grid" id="sudokuGrid">
                <!-- Wird dynamisch generiert -->
            </div>
        </div>
        
        <div class="number-pad">
            <button class="num-btn" onclick="enterNumber(1)">1</button>
            <button class="num-btn" onclick="enterNumber(2)">2</button>
            <button class="num-btn" onclick="enterNumber(3)">3</button>
            <button class="num-btn" onclick="enterNumber(4)">4</button>
            <button class="num-btn" onclick="enterNumber(5)">5</button>
            <button class="num-btn" onclick="enterNumber(6)">6</button>
            <button class="num-btn" onclick="enterNumber(7)">7</button>
            <button class="num-btn" onclick="enterNumber(8)">8</button>
            <button class="num-btn" onclick="enterNumber(9)">9</button>
            <button class="num-btn action" onclick="clearCell()">✕</button>
            <button class="num-btn action" onclick="useHint()">💡</button>
            <button class="num-btn action" onclick="backToLevels()">⬅</button>
        </div>
    </div>
    
    <!-- WIN OVERLAY -->
    <div class="win-overlay" id="winOverlay">
        <div class="heart-explosion">💖</div>
        <h2 class="win-text">BIBSIE LIEBE!<br>AKTIVIERT!</h2>
        <p class="win-subtext">ビブシーラブ発動!</p>
        <p style="color: white; font-size: 1.2rem; margin-bottom: 20px; text-align: center; padding: 0 20px;">
            "Die Zahlen haben gesprochen..."
        </p>
        <button class="btn" onclick="nextLevel()">WEITER ➜</button>
    </div>
    
    <!-- FINAL SCREEN -->
    <div class="screen" id="finalScreen">
        <div class="final-container">
            <h2 class="main-title" style="font-size: 2rem; margin-bottom: 20px;">SAGA COMPLETE!</h2>
            
            <div style="display: flex; align-items: center; justify-content: center; gap: 30px; flex-wrap: wrap;">
                <img src="hero.png" alt="Bibsie Boy" style="width: 100px; height: 150px; object-fit: contain; filter: drop-shadow(0 10px 20px rgba(255,45,85,0.6)); animation: heroFloat 3s infinite;">
                
                <div class="love-connection">💕</div>
                
                <div class="princess-emoji">👸</div>
            </div>
            
            <div class="credits-box">
                <h2>Prinzessin Mary wurde gerettet!</h2>
                <p>"Die größte Liebe ist die,<br>die durch Zahlen gefunden wurde"</p>
                <p style="margin-top: 15px; color: var(--gold);">ありがとうございました</p>
            </div>
            
            <button class="btn" onclick="resetGame()" style="margin-top: 20px;">NEUES SPIEL+</button>
        </div>
    </div>

    <script>
        // Video Intro Logik
        const video = document.getElementById('introVideo');
        const videoScreen = document.getElementById('videoScreen');
        
        // Wenn Video zu Ende, zeige Title Screen
        video.addEventListener('ended', function() {
            videoScreen.classList.add('hidden');
            document.getElementById('titleScreen').classList.add('active');
        });
        
        // Überspringen Funktion
        function skipVideo() {
            video.pause();
            videoScreen.classList.add('hidden');
            document.getElementById('titleScreen').classList.add('active');
        }
        
        // Game State
        let currentLevel = 1;
        let selectedCell = null;
        let gameGrid = [];
        let solutionGrid = [];
        let fixedCells = [];
        let timer = 0;
        let timerInterval = null;
        let completedLevels = JSON.parse(localStorage.getItem('bibsieCompleted')) || [];
        
        // Level Konfiguration
        const levels = [
            { name: "ROSA ANFANG", difficulty: "easy", color: "#FF6B9D" },
            { name: "NEON STORM", difficulty: "easy", color: "#FF2D55" },
            { name: "ENERGIE FLUSS", difficulty: "medium", color: "#FF1744" },
            { name: "DONNER DIMENSION", difficulty: "medium", color: "#D50000" },
            { name: "PLASMA ZONE", difficulty: "medium", color: "#C51162" },
            { name: "KOSMISCHER WAL", difficulty: "hard", color: "#AA00FF" },
            { name: "SUPERNOVA", difficulty: "hard", color: "#6200EA" },
            { name: "GALAXY GATE", difficulty: "hard", color: "#304FFE" },
            { name: "PRINZESSIN MARY", difficulty: "legend", color: "#FFD700" }
        ];
        
        // Sudoku Generator
        function generateSudoku(difficulty) {
            let grid = Array(9).fill().map(() => Array(9).fill(0));
            
            for (let i = 0; i < 9; i += 3) {
                fillBox(grid, i, i);
            }
            
            solveSudoku(grid);
            
            solutionGrid = grid.map(row => [...row]);
            
            let cellsToRemove = {
                'easy': 35,
                'medium': 45,
                'hard': 55,
                'legend': 60
            }[difficulty];
            
            let puzzle = grid.map(row => [...row]);
            let removed = 0;
            while (removed < cellsToRemove) {
                let row = Math.floor(Math.random() * 9);
                let col = Math.floor(Math.random() * 9);
                if (puzzle[row][col] !== 0) {
                    puzzle[row][col] = 0;
                    removed++;
                }
            }
            
            gameGrid = puzzle;
            fixedCells = [];
            for (let i = 0; i < 9; i++) {
                for (let j = 0; j < 9; j++) {
                    if (puzzle[i][j] !== 0) {
                        fixedCells.push(i * 9 + j);
                    }
                }
            }
        }
        
        function fillBox(grid, row, col) {
            let nums = [1, 2, 3, 4, 5, 6, 7, 8, 9];
            nums.sort(() => Math.random() - 0.5);
            let index = 0;
            for (let i = 0; i < 3; i++) {
                for (let j = 0; j < 3; j++) {
                    grid[row + i][col + j] = nums[index++];
                }
            }
        }
        
        function solveSudoku(grid) {
            for (let row = 0; row < 9; row++) {
                for (let col = 0; col < 9; col++) {
                    if (grid[row][col] === 0) {
                        for (let num = 1; num <= 9; num++) {
                            if (isValid(grid, row, col, num)) {
                                grid[row][col] = num;
                                if (solveSudoku(grid)) return true;
                                grid[row][col] = 0;
                            }
                        }
                        return false;
                    }
                }
            }
            return true;
        }
        
        function isValid(grid, row, col, num) {
            for (let x = 0; x < 9; x++) {
                if (grid[row][x] === num) return false;
            }
            for (let x = 0; x < 9; x++) {
                if (grid[x][col] === num) return false;
            }
            let startRow = row - row % 3;
            let startCol = col - col % 3;
            for (let i = 0; i < 3; i++) {
                for (let j = 0; j < 3; j++) {
                    if (grid[i + startRow][j + startCol] === num) return false;
                }
            }
            return true;
        }
        
        // UI Funktionen
        function showScreen(screenId) {
            document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
            document.getElementById(screenId).classList.add('active');
            
            if (screenId === 'levelScreen') {
                renderLevelSelect();
            }
        }
        
        function renderLevelSelect() {
            const grid = document.getElementById('levelGrid');
            grid.innerHTML = '';
            
            for (let i = 1; i <= 9; i++) {
                const btn = document.createElement('div');
                btn.className = 'level-btn';
                btn.innerHTML = `<div style="font-size: 1.2rem;">${i}</div><div style="font-size: 0.7rem; font-family: 'Noto Sans JP';">${levels[i-1].name.substring(0, 8)}</div>`;
                
                if (i === 9) {
                    btn.classList.add('final');
                    btn.innerHTML = '👸 PRINZESSIN MARY';
                }
                
                if (completedLevels.includes(i)) {
                    btn.classList.add('completed');
                    btn.onclick = () => startLevel(i);
                } else if (i === 1 || completedLevels.includes(i - 1)) {
                    btn.classList.add('available');
                    btn.onclick = () => startLevel(i);
                } else {
                    btn.classList.add('locked');
                    btn.innerHTML = '🔒';
                }
                
                grid.appendChild(btn);
            }
        }
        
        function startLevel(level) {
            currentLevel = level;
            document.getElementById('levelDisplay').textContent = `LEVEL ${level}`;
            document.getElementById('levelName').textContent = levels[level-1].name;
            document.getElementById('levelName').style.color = levels[level-1].color;
            
            generateSudoku(levels[level-1].difficulty);
            renderGrid();
            startTimer();
            
            showScreen('gameScreen');
        }
        
        function renderGrid() {
            const grid = document.getElementById('sudokuGrid');
            grid.innerHTML = '';
            
            for (let i = 0; i < 9; i++) {
                for (let j = 0; j < 9; j++) {
                    const cell = document.createElement('div');
                    cell.className = 'cell';
                    cell.dataset.index = i * 9 + j;
                    cell.dataset.row = i;
                    cell.dataset.col = j;
                    
                    if (gameGrid[i][j] !== 0) {
                        cell.textContent = gameGrid[i][j];
                        if (fixedCells.includes(i * 9 + j)) {
                            cell.classList.add('fixed');
                        }
                    }
                    
                    cell.onclick = () => selectCell(cell);
                    grid.appendChild(cell);
                }
            }
        }
        
        function selectCell(cell) {
            if (cell.classList.contains('fixed')) return;
            
            document.querySelectorAll('.cell').forEach(c => c.classList.remove('selected'));
            cell.classList.add('selected');
            selectedCell = cell;
        }
        
        function enterNumber(num) {
            if (!selectedCell) return;
            
            const row = parseInt(selectedCell.dataset.row);
            const col = parseInt(selectedCell.dataset.col);
            
            if (fixedCells.includes(row * 9 + col)) return;
            
            gameGrid[row][col] = num;
            selectedCell.textContent = num;
            selectedCell.classList.remove('error');
            
            if (!isValidMove(row, col, num)) {
                selectedCell.classList.add('error');
                shakeScreen();
            }
            
            checkWin();
        }
        
        function clearCell() {
            if (!selectedCell) return;
            const row = parseInt(selectedCell.dataset.row);
            const col = parseInt(selectedCell.dataset.col);
            
            if (fixedCells.includes(row * 9 + col)) return;
            
            gameGrid[row][col] = 0;
            selectedCell.textContent = '';
            selectedCell.classList.remove('error');
        }
        
        function isValidMove(row, col, num) {
            for (let x = 0; x < 9; x++) {
                if (x !== col && gameGrid[row][x] === num) return false;
            }
            for (let x = 0; x < 9; x++) {
                if (x !== row && gameGrid[x][col] === num) return false;
            }
            let startRow = row - row % 3;
            let startCol = col - col % 3;
            for (let i = 0; i < 3; i++) {
                for (let j = 0; j < 3; j++) {
                    if ((startRow + i !== row || startCol + j !== col) && 
                        gameGrid[startRow + i][startCol + j] === num) return false;
                }
            }
            return true;
        }
        
        function useHint() {
            for (let i = 0; i < 9; i++) {
                for (let j = 0; j < 9; j++) {
                    if (gameGrid[i][j] === 0) {
                        gameGrid[i][j] = solutionGrid[i][j];
                        const cell = document.querySelector(`[data-row="${i}"][data-col="${j}"]`);
                        cell.textContent = solutionGrid[i][j];
                        cell.style.background = 'rgba(0,200,83,0.4)';
                        setTimeout(() => {
                            cell.style.background = '';
                        }, 1000);
                        checkWin();
                        return;
                    }
                }
            }
        }
        
        function checkWin() {
            for (let i = 0; i < 9; i++) {
                for (let j = 0; j < 9; j++) {
                    if (gameGrid[i][j] !== solutionGrid[i][j]) return;
                }
            }
            
            clearInterval(timerInterval);
            
            if (!completedLevels.includes(currentLevel)) {
                completedLevels.push(currentLevel);
                localStorage.setItem('bibsieCompleted', JSON.stringify(completedLevels));
            }
            
            if (currentLevel === 9) {
                setTimeout(() => showFinal(), 1000);
            } else {
                setTimeout(() => showWin(), 500);
            }
        }
        
        function showWin() {
            createHeartParticles();
            document.getElementById('winOverlay').classList.add('active');
        }
        
        function nextLevel() {
            document.getElementById('winOverlay').classList.remove('active');
            if (currentLevel < 9) {
                startLevel(currentLevel + 1);
            }
        }
        
        function showFinal() {
            createHeartParticles();
            showScreen('finalScreen');
            setInterval(() => createHeartParticles(true), 2000);
        }
        
        function createHeartParticles(gold = false) {
            for (let i = 0; i < 20; i++) {
                setTimeout(() => {
                    const particle = document.createElement('div');
                    particle.className = 'particle heart-particle';
                    particle.textContent = gold ? '✨' : '💖';
                    particle.style.left = Math.random() * 100 + 'vw';
                    particle.style.top = '100vh';
                    particle.style.color = gold ? '#FFD700' : '#FF6B9D';
                    document.body.appendChild(particle);
                    
                    setTimeout(() => particle.remove(), 3000);
                }, i * 100);
            }
        }
        
        function shakeScreen() {
            document.body.style.animation = 'shake 0.5s';
            setTimeout(() => {
                document.body.style.animation = '';
            }, 500);
        }
        
        function startTimer() {
            clearInterval(timerInterval);
            timer = 0;
            timerInterval = setInterval(() => {
                timer++;
                const mins = Math.floor(timer / 60).toString().padStart(2, '0');
                const secs = (timer % 60).toString().padStart(2, '0');
                document.getElementById('timer').textContent = `${mins}:${secs}`;
            }, 1000);
        }
        
        function backToLevels() {
            clearInterval(timerInterval);
            showScreen('levelScreen');
        }
        
        function resetGame() {
            completedLevels = [];
            localStorage.removeItem('bibsieCompleted');
            showScreen('titleScreen');
        }
        
        // Tastatur Support
        document.addEventListener('keydown', (e) => {
            if (document.getElementById('gameScreen').classList.contains('active')) {
                if (e.key >= '1' && e.key <= '9') {
                    enterNumber(parseInt(e.key));
                } else if (e.key === 'Backspace' || e.key === 'Delete') {
                    clearCell();
                }
            }
        });
    </script>
</body>
</html>
